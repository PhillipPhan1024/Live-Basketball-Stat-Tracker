<h2>Add Player</h2>

<%= form_with model: @player do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit "Create Player" %>
<% end %>

<%= link_to "Back", players_path %>